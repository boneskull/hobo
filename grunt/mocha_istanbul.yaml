options:
  mask: '**/*.spec.js'
  mochaOptions:
    - '--compilers=js:babel/register'
  require:
    - test/fixture.js
  reportFormats:
    - lcov
    - text-summary
  excludes:
    - node_modules/**
